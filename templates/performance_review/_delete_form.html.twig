<form method="post" action="{{ path('app_performance_review_delete', {'id': performance_review.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ performance_review.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
