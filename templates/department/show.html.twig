{% extends 'base.html.twig' %}

{% block title %}Department{% endblock %}

{% block body %}
    <h1>Department</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ department.id }}</td>
            </tr>
            <tr>
                <th>DepartmentName</th>
                <td>{{ department.departmentName }}</td>
            </tr>
        </tbody>
    </table>

    <div style="display: flex;">
    <a class="btn btn-secondary" href="{{ path('app_department_index') }}">back to list</a>
    &nbsp;&nbsp;&nbsp;&nbsp;
    <a class="btn btn-primary" href="{{ path('app_department_edit', {'id': department.id}) }}">edit</a>
    &nbsp;&nbsp;&nbsp;&nbsp;
    {{ include('department/_delete_form.html.twig') }}
{% endblock %}
