{% extends 'base.html.twig' %}

{% block title %}Attendance{% endblock %}

{% block body %}
    <h1>Attendance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ attendance.id }}</td>
            </tr>
            <tr>
                <th>CheckInDateTime</th>
                <td>{{ attendance.checkInDateTime ? attendance.checkInDateTime|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CheckOutDateTime</th>
                <td>{{ attendance.checkOutDateTime ? attendance.checkOutDateTime|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>
<div style="display: flex;">
    <a class="btn btn-secondary" href="{{ path('app_attendance_index') }}">back to list</a>
    &nbsp;&nbsp;&nbsp;&nbsp;
    <a class="btn btn-primary" href="{{ path('app_attendance_edit', {'id': attendance.id}) }}">edit</a>
    &nbsp;&nbsp;&nbsp;&nbsp;
    {{ include('attendance/_delete_form.html.twig') }}
    </>
{% endblock %}
