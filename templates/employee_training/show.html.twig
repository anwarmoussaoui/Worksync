{% extends 'base.html.twig' %}

{% block title %}EmployeeTraining{% endblock %}

{% block body %}
    <h1>EmployeeTraining</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employee_training.id }}</td>
            </tr>
            <tr>
                <th>CompletionDate</th>
                <td>{{ employee_training.completionDate ? employee_training.completionDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div style="display: flex;">
    <a class="btn btn-secondary" href="{{ path('app_employee_training_index') }}">back to list</a>
    &nbsp;&nbsp;&nbsp;&nbsp;
    <a class="btn btn-primary" href="{{ path('app_employee_training_edit', {'id': employee_training.id}) }}">edit</a>
    &nbsp;&nbsp;&nbsp;&nbsp;
    {{ include('employee_training/_delete_form.html.twig') }}
{% endblock %}
